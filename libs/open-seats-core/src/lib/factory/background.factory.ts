import { v4 as uuidv4 } from 'uuid';
import Konva from 'konva';
import { Seat, SeatConfig } from '../objects';
import { EngineState } from '../engine/engine.state';

export class BackgroundFactory {
	public createSeat(imageUrl: string): Seat {

		return seat;
	}
}
